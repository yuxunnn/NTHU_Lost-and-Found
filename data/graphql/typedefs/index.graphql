# Custom Scalars
scalar Date

type Query {
  # Version
  version: String!

  # User
  users: [User]
  userById(userId: ID!): User
  userByName(userName: String!): User

  # Post
  # posts: [Post]
  # post(postType: String!, itemType: String!, location: String): Post
}

type Mutation{
  createUser(user: UserCreateInput!): User!
}